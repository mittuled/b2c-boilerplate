import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { {{pascalCase name}} } from './{{kebabCase name}}';

describe('{{pascalCase name}}', () => {
  it('renders without crashing', () => {
    render(<{{pascalCase name}}>Hello</{{pascalCase name}}>);
    expect(screen.getByRole('region')).toBeTruthy();
  });

  it('applies a custom aria-label', () => {
    render(<{{pascalCase name}} label="Custom label">Content</{{pascalCase name}}>);
    expect(screen.getByLabelText('Custom label')).toBeTruthy();
  });

  it('renders children', () => {
    render(<{{pascalCase name}}>Test content</{{pascalCase name}}>);
    expect(screen.getByText('Test content')).toBeTruthy();
  });
});
